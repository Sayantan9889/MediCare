import { CartBadgePipe } from './cart-badge.pipe';

describe('CartBadgePipe', () => {
  it('create an instance', () => {
    const pipe = new CartBadgePipe();
    expect(pipe).toBeTruthy();
  });
});
